name: Docker
on:
  schedule:
    - cron: '0 */2 * * *'
  pull_request:
    branches:
      - master
  workflow_dispatch:
  
version: '3'
services:
  thetvapp-m3u:
    image: dtankdemp/thetvapp-m3u:latest
    container_name: thetvapp-m3u
    ports:
        - 4124:4124 
    restart: unless-stopped
