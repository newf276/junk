name: ripper
on:
  schedule:    
    - cron: "0 */3 * * *"
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Downloading ripper file...
      run: |
        echo Downloading Files...
        exec 3>&1 &>/dev/null
        cd /newf276/junk/pull/
        chmod +x download.sh
        /newf276/junk/pull/download.sh

